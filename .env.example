# Copy to .env and set values for your environment.
# Defaults to localhost if not set (correct for running on the same host as the Docker stack).
# MQTT_BROKER=100.95.134.66   # Tailscale IP when running remotely from the broker
# MQTT_PORT=1883               # Optional — 1883 is the default
